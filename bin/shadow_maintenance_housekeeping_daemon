#!/home/kloros/.venv/bin/python3
"""
Shadow Daemon - maintenance_housekeeping

Runs 24-hour shadow mode validation for maintenance_housekeeping niche.
"""

import sys
import logging
from pathlib import Path

# Use kloros home explicitly (not current user's home)
sys.path.insert(0, '/home/kloros/src')

from kloros.dream.shadow_daemon import main_maintenance_housekeeping

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)

if __name__ == "__main__":
    sys.exit(main_maintenance_housekeeping())

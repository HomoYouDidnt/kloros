# pytest.ini
[pytest]
addopts = -n auto --maxfail=3 -q -ra --basetemp=/home/kloros/tmp/pytest --json-report --json-report-file=/home/kloros/logs/pytest_latest.json
# Tag management:
markers =
    slow: slow tests (skipped in fast filter)
    e2e: end-to-end tests (skipped in fast filter)
